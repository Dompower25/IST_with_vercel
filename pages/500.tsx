import { FC } from 'react'

const ServerErrorPage: FC = () => {
  return (
    <div
      style={{
        color: '#fff',
      }}
    >
      INITIAL SERVER ERROR
    </div>
  )
}

export default ServerErrorPage
